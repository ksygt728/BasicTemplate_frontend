/**
 * @파일명 : loading.tsx
 * @설명 : 전역 로딩 페이지 (신규 테마 적용)
 * @작성일 : 2025.12.08
 */

import { Loading } from "@/components/common/themed";

export default function LoadingPage() {
  return <Loading message="Loading..." />;
}
